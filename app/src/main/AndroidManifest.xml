<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sonyvaio.learnjava">

    <application
        android:name=".QuestoesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_learn"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ConceitosBasicosActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LacosActivity" />
        <activity android:name=".OrientacaoObjetosActivity"></activity>
    </application>

</manifest>
